services:
  postgres:
    image: 'postgres:17.5-alpine'
    environment:
      - POSTGRES_DB=pickdate
      - POSTGRES_USER=root
      - POSTGRES_PASSWORD=pass

    ports:
      - '5432:5432'
    networks:
      - pickdate

networks:
  pickdate:
    driver: bridge
